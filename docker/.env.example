# Estas variables las lee docker-compose.yml directamente.
# Deben coincidir con DB_DATABASE / DB_USERNAME / DB_PASSWORD en backend/.env

DB_DATABASE=inside_cancun
DB_USERNAME=inside_cancun
DB_PASSWORD=secret
